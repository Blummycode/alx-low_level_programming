#include "main.h"
#include <string.h>
/**
*_strspn - gets the length of a prefix substring
*@s: pointer to the string to be searched
*@accept: prefix measured
*
*Return: Always 0(success)
*/
unsigned int _strspn(char *s, char *accept)
{
	int n;

	n = strspn(char *s, char *accept);
	return (0);
}
