#include <stdio.h>
/**
 * main - main prints alphabet in lowercase
 *
 * Return: Always 0 (success)
 *
 */
int main(void)
{
	char c;
	
	for (c = 'a'; c <= 'z'; c++);
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
