#include "main.h"
/**
 * _abs - return absolute value of an int
 * @n: the int to be checked
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
